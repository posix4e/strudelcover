# Core ML packages for audio analysis
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.35.0
audiocraft>=1.0.0
demucs>=4.0.0
basic-pitch>=0.3.0

# Additional audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0
numpy<2.0.0  # Keep numpy < 2.0 for compatibility

# Utils
tqdm>=4.65.0
pydub>=0.25.0
essentia>=2.1b6.dev1105  # Optional, for advanced analysis
pretty-midi>=0.2.10
mir-eval>=0.7

# For MusicGen/AudioCraft
xformers>=0.0.22  # Optional, for faster inference
einops>=0.7.0
hydra-core>=1.3.0